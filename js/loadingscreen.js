let spinnerWrapper = document.querySelector('.spinner-wrapper');


window.addEventListener('load', function () {
    ///document.querySelector('.spinner-wrapper').classList.add('fadeOut');
    setTimeout(() => {spinnerWrapper.style.display = 'none'; }, 2500);
    
});